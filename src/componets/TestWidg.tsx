import React, {
  useEffect,
  useState,
  FC,
  useMemo,
  forwardRef,
  useRef,
  memo,
} from "react";

// interface WidgetProps {
//   id: string;
// }
// export const Widget = forwardRef(({ id, ref }): WidgetProps => {
//   return <div ref={ref}>{id}</div>;
// });
// Widget.displayName = "MyWidget";
// export default memo(Widget);
